# CLAUDE.md

このリポジトリは HTML5 ゲーム『ERLAB ―その数値を見逃すな―』の開発用です。
作業前に必ず `docs/design.md`・`docs/characters.md`・`docs/roadmap.md`・`docs/mechanics.md`・`docs/readable.md` を読んでください。設計判断はそこに集約しています。症例ごとの仕様は `docs/case07.md` のように症例名で置く。

## 守ること
- 医師パート・病名確定・治療選択は実装しない（検査室特化）
- 疑いタブ（`data/suspects.json`）に並べるのは検体・検査室側の見立てだけ。病名は並べない
- 報告に載るのはマークした行だけ。マーク0件でも報告はできる（＝異常なしの報告）
- 自由記述の入力欄を作らない（評価できない・スマホでつらい）。隠すのではなく置かない。
  報告のコメントは `data/comment_templates.json` の**所見一覧**から選ぶ。一〜三行まで。
  一覧は全症例で同じで、常に全部出す（症例ごとに書き足さない）。仕様は `docs/comment-list.md`
- 用語辞典（`data/glossary.json`）は一語三行以内。「何か／なぜ値がずれるか／何を見れば分かるか」の順。
  病名は書かない。長くなるなら逆引き辞典（roadmap 4章）の仕事
- 索引パネルの「見る順番」は答えではなく手順。全症例で同じ五行。上級モードでも消さない
- 指さし（`nav` の `focus`）は一文に一か所だけ。点滅させない（割り込みの点滅と区別する）。
  症例4以降には書かない。演出であって、判定には一切効かない。
  症例0は自動で進み、症例1〜3は文末の「ここ」を押して出す。どちらも枠は押し直す（進む）まで残す
- 検査値の派生値は必ず `src/derive.js` で計算する。症例JSONに派生値を直書きしない
- 基準範囲・パニック値は `data/hospital.json` の1セットのみ。数値を追加・変更したら `docs/sources.md` に出典を書く
- `data/artifacts.json` の影響量（溶血の段階ごとの上乗せなど）は固定。症例を成立させるために変更しない。見せたい値にならないときは症例側の素の値（`conditions.json` か `seed.overrides`）で合わせる
- 索引（用語集）は5枠固定: 何を測るか / 基準範囲 / 高いと / 低いと / 検体トラブルで偽値が出る条件
- ナビは指導役（`data/mentors.json`）ごとに言い方を変えてよいが、教える中身は変えない。どちらを選んでも同じ判断にたどり着けること
- 台詞の原文は `docs/dialogue.md`。実装に流すときは speaker と emotion を必ず付ける（emotion は `assets/portraits/{speaker}_{emotion}.png` に対応）
- 報告後は「（あれば）指導役の講評 → 医師の返信」の順。医師の返信はどの分岐にも必ず付ける。指導役の講評はその症例で教えたい判断に関わる分岐だけで、分岐を埋めるためだけに台詞を書き足さない
- 医師の差し戻し（`then` / `cap` / `followup`）は手書きの症例だけで起こす。ランダム化しない。医師を悪者にしない
- 「医師に反論する」入力欄は作らない。プレイヤーが返すのは報告レベルであって言葉ではない
- 異常表示の主はフラグ記号（H, L, HH, LL）。色は補助
- 危険域（パニック値）は**値の話としてだけ**画面に出す。結果の行に「6.0以上は危険域」の形で境目を書き、
  フラグ欄の HH / LL には「パニック値」の札を添える。**行動（電話・緊急・再採血）は画面に書かない。**
  同じ HH でも検体状態しだいで答えが変わる（症例5は再採血、症例7は再度電話）ため、
  「HH を見たら電話」という反射を作らない。札は HH / LL だけで、H / L には付けない
- **色を塗ってよいのは次の2か所だけ**
  1. フラグ欄のセル（H/L 淡い黄、HH/LL 淡い赤）
  2. 検体状態欄。ただしコメントが入っているときだけ。空欄のときは何もしない
- 結果値・項目名・行全体・正常セルは塗らない。背景も枠線も付けない
- **例外は「選択状態」「画面状態」「説明の指し示し」「済んだ検体」の4つだけ**（値の判定ではないので色ルールの外）
  1. マークした行の反転（暗い地に明るい文字）。報告に載せる行を選んだ、という表示。
     印欄の記号（✓）と `aria-pressed` を必ず併せて出し、反転を消しても選択が分かるようにする
  2. 割り込み中のヘッダーバーの赤と、受付一覧で割り込んだ行。ERから至急が入っている画面状態の
     合図で、値の重さではない。「至急対応中」「至急⚑」の文字を必ず併せて出し、点滅は数秒でやめる
  3. 指さしの黄色い枠（`--point`）。いま説明している場所はここ、という指し示しで、値の判定ではない。
     一度に一か所。赤は使わない（割り込みの合図とぶつかる）。点滅させず、現れるときに0.3秒で線を引くだけ。
     枠は時間で消さない——症例0は次の文に進むまで、症例1〜3は次の「ここ」を押すまで。症例を移ると消す
  4. 受付一覧で、報告して**閉じた**検体の灰色。もう手を離してよい、という状態の表示で、値の判定ではない。
     「報告済」の文字と、一覧の下へ流れることを必ず併せて出す。閉じても一覧から消さない（開いて読み返せる）。
     灰色にするのは症例が閉じたときで、報告したときではない（症例7の一本目は報告しても閉じない）
- 色は「ここに異常がある」ことだけを示す合図。異常の種類も重症度も色では伝えない。
  それを伝えるのはフラグ記号と数値であり、色を消しても同じ判断ができる状態を保つ
- 色以外の手がかり（太字・枠線・記号）を必ず併せて付ける
- 音は**出来事**（結果が届いた・メッセージ・タップ・送信・電話）にだけ付ける。
  値の重さ（HH / LL・危険域・Δ）にも判定の良し悪しにも付けない。色と同じで、音を切っても
  同じ判断ができる状態を保つ。音声ファイルは置かず `src/sound.js` で合成する。仕様は `docs/sound.md`
- 新人モードは時間無制限。キュー管理や時間制限は上級モードまで持ち込まない
- 医学教育教材を名乗る文言を入れない

## 構成
- `index.html` — エントリ
- `src/` — ロジック（derive.js, lis.js, messages.js, report.js など）
- `data/` — JSONデータ（tests, conditions, artifacts, hospital, cases/, messages/）
- `docs/` — 設計メモ・出典

## 進め方
- `docs/design.md` の「7. 最初のマイルストーン」の順で進める
- 画面はスマホ縦でも崩れないこと（PLiCy想定）
- 依存ライブラリは原則なし
